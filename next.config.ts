import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // async redirects() {
  //   return [
  //     {
  //       source: '/',
  //       destination: '/homepage',
  //       permanent: true, // Set to true for permanent 301 redirect or false for temporary 302 redirect
  //     },
  //   ];
  // },
};

export default nextConfig;
